# Résumés

Drop the **vetted, public-safe** CV here as `Prateek_Jha_CV.pdf` (path referenced by
`lib/contact.ts`). It must be scrubbed of client/employer names before it ships —
the site is public. The "Résumé" link in the Contact section points to it.

Persona-specific variants (fintech-pm, healthcare-sf, etc.) are deferred post-launch.
